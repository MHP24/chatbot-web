import { Controller, Get, Query } from '@nestjs/common';

@Controller('chats')
export class ChatsController {
  // @Get('request')
  // requestChat(@Query('chatId') chatId: string) {
  //   //.. handleChatRequest chatId
  //   return 'asd';
  // }
}
