export const ChatHeader = () => {
  return (
    <header>
      {/* Current status (online, offline) & flow level (Chatbot, Agent) */}
      <div>

      </div>

      {/* Actions (Close, reload) */}
      <div>

      </div>
    </header>
  )
}
